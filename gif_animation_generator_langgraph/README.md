# GIF Animation Generator (LangGraph + GPT-4 + DALL-E)

This project converts a Jupyter notebook into a clean, production-ready VS Code Python app.

## Features
- LangGraph workflow
- GPT-4 prompt generation
- DALL-E image creation
- Automatic GIF generation
- Async execution

## Run

1. Create venv:
